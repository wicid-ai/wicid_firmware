settings:
  commit:
    prefix: 'build'
    subject: 'update releases.json from <%- repository %>'
  pull_request:
    disabled: true  # Push directly to main, no PR needed

patterns:
  - files:
      - from: releases.json
        to: public/releases.json
    repositories:
      - wicid-ai/wicid_web@main

